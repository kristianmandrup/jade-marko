extends package::global/layout.jade

block title
  title Article Title

block content
  h1 My Article

  async-fragment(data="globalData.xxx")

  xvar(a='17')
  xif(test='a = b')
    $globalData.abc.x
  xelse-if(test='a = c')
    $data.c
  xelse
    $data.d
